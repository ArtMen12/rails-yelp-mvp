<div class="container">
  <h1>Nouvelle critique</h1>

  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.submit "Ajouter cette critique", class: "btn btn-primary" %>
  <% end %>
</div>
